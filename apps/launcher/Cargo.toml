[package]
name = "oos-launcher"
version = "0.1.0"
edition = "2021"
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
egui = { version = "0.35.0", default-features = false, features = ["default_fonts"] }
oos-app = { path = "../../sdk/rust/oos-app" }
oos-egui = { path = "../../sdk/rust/oos-egui" }
