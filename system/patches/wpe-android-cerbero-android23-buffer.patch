diff --git a/recipes/wpebackend-android.recipe b/recipes/wpebackend-android.recipe
index 12691ff..8d3384f 100644
--- a/recipes/wpebackend-android.recipe
+++ b/recipes/wpebackend-android.recipe
@@ -1,5 +1,6 @@
 # -*- Mode: Python -*- vi:si:et:sw=4:sts=4:ts=4:syntax=python
 
 from cerbero.utils import android
+from cerbero.enums import DistroVersion
 
 class Recipe(recipe.Recipe):
@@ -10,5 +11,9 @@ class Recipe(recipe.Recipe):
     remotes = {'origin': 'https://github.com/Igalia/WPEBackend-android.git'}
     commit = '60405476655123dec147f5008cef1bef4a12ce3b'
     deps = ['glib', 'libwpe']
+
+    patches = [
+        'wpebackend-android/0001-OOS-RGB565-and-Android23-buffer.patch',
+    ]
 
     files_libs = ['libWPEBackend-android']
@@ -30,2 +31,7 @@ class Recipe(recipe.Recipe):
         stl_libdir = os.path.join(self.config.toolchain_prefix, 'libs', arch_lib_name)
         self.append_env('LDFLAGS', '-L' + stl_libdir, '-lc++_shared', '-llog', '-lgobject-2.0', '-O2')
+        api = DistroVersion.get_android_api_version(self.config.target_distro_version)
+        if api < 26:
+            compat = os.path.join(self.config.prefix, 'lib', 'liboos-android23-buffer.so')
+            self.configure_options += ' -DOOS_ANDROID_LEGACY_BUFFER=ON'
+            self.configure_options += ' -DOOS_ANDROID_LEGACY_BUFFER_LIBRARY=' + compat
+            self.append_env('CXXFLAGS', '-DOOS_ANDROID_LEGACY_BUFFER=1')
diff --git a/recipes/wpewebkit.recipe b/recipes/wpewebkit.recipe
index c7d14aa..167520f 100644
--- a/recipes/wpewebkit.recipe
+++ b/recipes/wpewebkit.recipe
@@ -1,3 +1,5 @@
 # -*- Mode: Python -*- vi:si:et:sw=4:sts=4:ts=4:syntax=python
+
+from cerbero.enums import DistroVersion
 
 class Recipe(recipe.Recipe):
@@ -114,4 +116,5 @@ class Recipe(recipe.Recipe):
         'wpewebkit/0001-WPE-Decouple-touch-driven-scrolling-unconditionally.patch',
         'wpewebkit/0002-KaiOS-Android-use-GioUnix.patch',
         'wpewebkit/0003-KaiOS-Android-ARMv7-JIT.patch',
+        'wpewebkit/0004-KaiOS-Android23-legacy-buffer.patch',
     ]
@@ -163,3 +166,9 @@ class Recipe(recipe.Recipe):
             self.append_env('CXXFLAGS', '-DXR_USE_PLATFORM_ANDROID')
+            api = DistroVersion.get_android_api_version(self.config.target_distro_version)
+            if api < 26:
+                compat = os.path.join(self.config.prefix, 'lib', 'liboos-android23-buffer.so')
+                self.configure_options += ' -DOOS_ANDROID_LEGACY_BUFFER_LIBRARY=' + compat
+                self.append_env('CXXFLAGS', '-DOOS_ANDROID_LEGACY_BUFFER=1',
+                    '-D__ANDROID_UNAVAILABLE_SYMBOLS_ARE_WEAK__=1')
             # Placeholder to allow enabling debug externally
             self.append_env('WEBKIT_DEBUG', '')
