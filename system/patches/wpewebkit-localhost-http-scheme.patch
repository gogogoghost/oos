From f82a93c257304e5e67dd370450577ec9b7ac2b02 Mon Sep 17 00:00:00 2001
From: OOS <oos@example.invalid>
Date: Thu, 30 Jul 2026 20:16:00 +0800
Subject: [PATCH] WPE: route localhost application origins to scheme handlers

Allow the OOS package source to serve port-free KaiOS 3 localhost origins
without intercepting ordinary HTTP traffic.
---
diff --git a/Source/WebKit/UIProcess/API/glib/WebKitWebContext.cpp b/Source/WebKit/UIProcess/API/glib/WebKitWebContext.cpp
--- a/Source/WebKit/UIProcess/API/glib/WebKitWebContext.cpp
+++ b/Source/WebKit/UIProcess/API/glib/WebKitWebContext.cpp
@@ -1387,7 +1387,7 @@ void webkit_web_context_register_uri_scheme(WebKitWebContext* context, const ch
         return;
     }
 
-    if (WTF::URLParser::isSpecialScheme(canonicalizedScheme.value())) {
+    if (WTF::URLParser::isSpecialScheme(canonicalizedScheme.value()) && canonicalizedScheme.value() != "http"_s) {
         g_warning("Registering special URI scheme %s is no longer allowed", scheme);
         return;
     }
diff --git a/Source/WebKit/UIProcess/WebPageProxy.cpp b/Source/WebKit/UIProcess/WebPageProxy.cpp
--- a/Source/WebKit/UIProcess/WebPageProxy.cpp
+++ b/Source/WebKit/UIProcess/WebPageProxy.cpp
@@ -15367,7 +15367,7 @@ void WebPageProxy::setURLSchemeHandlerForScheme(Ref<WebURLSchemeHandler>&& hand
 {
     auto canonicalizedScheme = WTF::URLParser::maybeCanonicalizeScheme(scheme);
     ASSERT(canonicalizedScheme);
-    ASSERT(!WTF::URLParser::isSpecialScheme(canonicalizedScheme.value()));
+    ASSERT(!WTF::URLParser::isSpecialScheme(canonicalizedScheme.value()) || canonicalizedScheme.value() == "http"_s);
 
     auto schemeResult = m_urlSchemeHandlersByScheme.add(canonicalizedScheme.value(), handler.get());
     ASSERT_UNUSED(schemeResult, schemeResult.isNewEntry);
diff --git a/Source/WebKit/WebProcess/Network/WebLoaderStrategy.cpp b/Source/WebKit/WebProcess/Network/WebLoaderStrategy.cpp
--- a/Source/WebKit/WebProcess/Network/WebLoaderStrategy.cpp
+++ b/Source/WebKit/WebProcess/Network/WebLoaderStrategy.cpp
@@ -337,6 +337,9 @@ bool WebLoaderStrategy::tryLoadingUsingURLSchemeHandler(ResourceLoader& resourc
     if (!handler)
         return false;
 
+    if (resourceLoader.request().url().protocolIs("http"_s) && !resourceLoader.request().url().host().endsWithIgnoringASCIICase(".localhost"_s))
+        return false;
+
     LOG(NetworkScheduling, "(WebProcess) WebLoaderStrategy::scheduleLoad, URL '%s' will be handled by a UIProcess URL scheme handler.", resourceLoader.url().string().utf8().data());
     WEBLOADERSTRATEGY_RELEASE_LOG("tryLoadingUsingURLSchemeHandler: URL will be handled by a UIProcess URL scheme handler");

@@ -781,6 +784,9 @@ std::optional<WebLoaderStrategy::SyncLoadResult> WebLoaderStrategy::tryLoading
     if (!handler)
         return std::nullopt;
 
+    if (request.url().protocolIs("http"_s) && !request.url().host().endsWithIgnoringASCIICase(".localhost"_s))
+        return std::nullopt;
+
     LOG(NetworkScheduling, "(WebProcess) WebLoaderStrategy::scheduleLoad, sync load to URL '%s' will be handled by a UIProcess URL scheme handler.", request.url().string().utf8().data());
 
     SyncLoadResult result;
